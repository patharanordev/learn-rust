# Cargo caching handler

`cargo` will caching dependency into `.cargo`, you may got warning message **no space left on device**. Let's clear it via `cargo-cache` CLI.

install:

```sh
cargo install cargo-cache
```

To clear all:

```sh
cargo cache -a
```
