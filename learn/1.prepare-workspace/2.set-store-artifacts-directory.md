# Set store artifacts directory

Normally cargo will generate output artifact into `./target`.

You can change it via environment variable name `CARGO_TARGET_DIR` if need, ex. got directory permission issue or others.
